apiVersion: v1
kind: Service
metadata:
  name: bitcoin-fetcher
  annotations:
    prometheus.io/scrape: "true"
    prometheus.io/path: "/metrics"
    prometheus.io/port: "8000"
spec:
  selector:
    app: bitcoin-fetcher
  ports:
  - port: 80
    targetPort: 8000
    name: metrics
  type: ClusterIP