- LOAD DATA AND PARSE TIME CORRECTLY: convert the time column to a true datetime, sort by time, and SET IT AS INDEX (or keep a dedicated time column if the data is panel/long format). FOCUS: the mechanics of turning a raw timestamp column into a reliable time axis that all later plots/resampling depend on. [ingest] [time-index] [essential]
- VERIFY BASIC INTEGRITY: check row counts, duplicate timestamps, duplicate entity+timestamp pairs (for panel data), unexpected negative values (if impossible), and impossible jumps. [ingest] [sanity] [essential]
- IDENTIFY THE SERIES STRUCTURE: classify as (A) single series, (B) multiple series (panel: entity_id + time), or (C) multivariate (many sensors/features per time). Decide the PRIMARY KEY (time) and SECONDARY KEY (entity_id) early. [ingest] [schema] [essential]
- MAP FEATURE TYPES: separate NUMERIC CONTINUOUS, NUMERIC COUNTS, BINARY FLAGS, CATEGORICALS, and KNOWN EXOGENOUS (calendar, price, weather, events). Keep a clean list of TARGET vs COVARIATES if applicable. [ingest] [feature-typing] [essential]
- CHECK TIME RANGE + COVERAGE: compute min/max timestamps, expected frequency, and missing time coverage per entity (coverage %, longest gap, number of gaps). [ingest] [coverage] [essential]
- INFER/CONFIRM SAMPLING FREQUENCY: compute typical time deltas (mode/median), detect IRREGULAR SAMPLING, and decide whether to RESAMPLE to a regular grid or keep irregular with gap-aware methods. [ingest] [frequency] [essential]

- AUDIT MISSINGNESS TWO WAYS: (1) missing VALUES inside existing timestamps, (2) missing TIMESTAMPS (holes in the grid). Track both, because they imply different fixes and different model risks. [quality] [missingness] [essential]
- HANDLE MISSING VALUES WITH INTENT: choose one of: LEAVE AS NaN + add missingness flags, FORWARD-FILL, INTERPOLATE, ZERO-FILL (only for true counts with meaning), or DROP segments. ALWAYS record what you did and why. [quality] [imputation] [essential]
- STANDARDIZE UNITS AND SCALE: if features are on wildly different scales (common in multivariate sensors), consider robust scaling (median/IQR) or log1p for heavy-tailed positive variables. [quality] [scaling] [optional]

- START WITH UNIVARIATE SUMMARIES: for each numeric feature compute count/mean/std/min/max/quantiles; for panel data do this PER-ENTITY and overall to detect “weird entities”. [univariate] [stats] [essential]
- PLOT UNIVARIATE DISTRIBUTIONS: histogram/KDE (or ECDF) for each feature; check SKEW, HEAVY TAILS, ZERO-INFLATION, and MULTI-MODALITY. FOCUS: quickly learning “what values look normal” and whether transformations will be needed (common for demand/sales/count targets). [univariate] [distribution] [essential]
- TEST / FIT DISTRIBUTIONS WHEN IT MATTERS: if you need transformations or probabilistic assumptions, compare candidate fits (e.g., log-normal / gamma / Johnson family) and choose a transformation accordingly. FOCUS: using a “best-fit distribution” check as a practical trigger for log/Box-Cox-like transforms, not as a purity test. [univariate] [assumptions] [optional]

- PLOT THE RAW TIME SERIES FIRST: line plot of target(s) over time; for multivariate data use SMALL MULTIPLES (grid of plots) so you can compare shapes and volatility. FOCUS: seeing overall trend + abrupt regime changes across many variables at once. [time-view] [trend-shifts] [essential]
- ZOOM INTO WINDOWS: always inspect multiple windows (last week/month, a random week, a random day) to detect EVENT SPIKES, DRIFT, regime changes, and sensor dropouts that disappear at full scale. [time-view] [zoom] [essential]
- MARK EVENTS / REGIMES VISUALLY: add vertical lines or shaded spans for known events, policy changes, maintenance periods, holidays, outages, or dataset-defined “eras”. FOCUS: adding HUMAN-INTERPRETABLE ANCHORS so patterns aren’t misread as “random noise.” [time-view] [events] [optional]

- RESAMPLE TO MULTIPLE GRAINS: compute aggregates at several frequencies (hourly→daily→weekly→monthly) using mean/sum/max depending on variable type; compare plots to separate NOISE vs SIGNAL. FOCUS: the workflow of resampling + plotting to reveal structure hidden at the native sampling rate. [time-view] [resampling] [essential]
- CHECK SEASONALITY EXPLICITLY: compute average by HOUR-OF-DAY, DAY-OF-WEEK, WEEK-OF-YEAR, MONTH; plot seasonal subseries; look for multiple seasonalities (daily + weekly + yearly). FOCUS: practical “seasonality discovery” using grouped averages and seasonal plots. [seasonality] [cycles] [essential]
- DECOMPOSE WHEN USEFUL: use a decomposition (trend/seasonal/residual) to sanity-check that seasonality is stable and residuals look “noise-like”. FOCUS: separating TREND vs SEASONAL vs RESIDUAL to avoid blaming seasonality for anomalies (or vice versa). [seasonality] [decomposition] [optional]

- COMPUTE ROLLING STATISTICS: rolling mean/median/std/min/max (multiple window sizes) to reveal CHANGING VARIANCE, local trend, and volatility clustering. FOCUS: using rolling summaries as a fast drift/volatility detector and as a design guide for lag/rolling features later. [dynamics] [rolling] [essential]
- DETECT OUTLIERS AS “TIME EVENTS”, NOT JUST POINTS: flag spikes, level shifts, stuck-at plateaus, and sudden variance changes; inspect BEFORE/AFTER context windows; decide whether to cap, remove, or label. [quality] [anomalies] [essential]

- COMPARE GROUPS (PANEL DATA): group by entity/category (store/item/device/customer) and compare distributions (boxplots) and time patterns; identify “hard entities” with unusual drift or noise. FOCUS: using per-entity comparisons (boxplots + time series) to find heterogeneity and segment-specific behavior. [panel] [group-compare] [essential]
- CHECK CROSS-FEATURE CORRELATIONS (MULTIVARIATE): compute correlation matrix (overall and per-window), and look for redundant sensors (near-duplicates) and sign flips over time (regime-dependent relationships). [multivariate] [correlation] [essential]
- CHECK LAG RELATIONSHIPS: ACF/PACF for key series; cross-correlation between features to spot LEAD–LAG effects and plausible lagged predictors. FOCUS: identifying “useful memory length” (how far back lags matter) and lead/lag coupling between variables. [dynamics] [lags] [optional]

- TEST STATIONARITY ONLY IF YOUR NEXT STEPS ASSUME IT: run ADF/KPSS and/or inspect rolling mean/variance; if non-stationary and you plan classical models, apply differencing or seasonal differencing and re-check. FOCUS: treating stationarity checks as a MODEL-ASSUMPTION GATE, not a required ritual. [model-readiness] [stationarity] [optional]
- VALIDATE LEAKAGE RISKS: ensure no future information is included in features (especially engineered lags/rolling); verify that any normalization/scaling is fit ONLY ON PAST data when modeling. [model-readiness] [leakage] [essential]
- CREATE A MINIMAL BASELINE FEATURE SET: calendar features + a few lags + rolling stats; verify they align correctly with time (no off-by-one). This doubles as an EDA sanity check. [model-readiness] [features] [optional]

- SUMMARIZE FINDINGS AS ACTIONABLE DECISIONS: record (1) frequency choice, (2) missingness strategy, (3) main seasonalities, (4) anomaly types present, (5) stable vs drifting features, (6) which entities/features are problematic. [wrap-up] [decisions] [essential]
- ALWAYS KEEP REPRODUCIBLE PLOTS + METRICS: store the exact parameters for resampling windows, rolling windows, anomaly thresholds, and any filtering so the agent can rerun and compare across datasets. [wrap-up] [reproducibility] [essential]
